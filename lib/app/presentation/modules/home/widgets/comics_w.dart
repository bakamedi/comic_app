import 'package:flutter/material.dart';

class ComicsW extends StatelessWidget {
  const ComicsW({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
